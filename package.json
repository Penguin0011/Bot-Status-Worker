{
  "name": "bot-status-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker + Durable Object that tracks bot heartbeats and exposes status, health and history endpoints",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir=.wrangler/dry-run",
    "test": "node --test \"test/**/*.test.mjs\""
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
